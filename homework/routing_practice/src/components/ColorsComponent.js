
import React from 'react';
const ColorsComponent= props => {
    return(
        <p>The first color is {props.color1}, then the second color is {props.color2}</p>
    )
}

export default ColorsComponent;